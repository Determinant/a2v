Put ``*.key`` and ``*.crt`` files in this directory, or generate new files by
running ``a2v genKey <node-id>``.  For the example ``validators.json``, files
such as ``staker0.crt`` and ``staker0.key`` should exist.
